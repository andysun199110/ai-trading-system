FROM python:3.11-slim
WORKDIR /app
COPY pyproject.toml /app/
RUN pip install --no-cache-dir -e .[dev]
COPY . /app
CMD ["uvicorn", "services.api_server.main:app", "--host", "0.0.0.0", "--port", "8000"]
