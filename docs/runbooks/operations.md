# Runbook

- Env setup: `.env` with JWT, DB, Telegram secrets.
- Promote deploys through `/admin/deployments/promote`.
- On subscription expiry, entries/signals are blocked; protective management continues.
